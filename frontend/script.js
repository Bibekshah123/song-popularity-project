const API_URL="http://127.0.0.1:8000/predict";const form=document.getElementById("predictionForm");const resultBox=document.getElementById("result");const errorBox=document.getElementById("errorBox");const predictBtn=document.getElementById("predictBtn");function getN(id){return Number(document.getElementById(id).value)}form.addEventListener("submit",async e=>{e.preventDefault();resultBox.classList.add("hidden");errorBox.classList.add("hidden");predictBtn.disabled=true;predictBtn.textContent="Predicting...";const p={acousticness:getN("acousticness"),danceability:getN("danceability"),energy:getN("energy"),instrumentalness:getN("instrumentalness"),key:getN("key"),liveness:getN("liveness"),loudness:getN("loudness"),audio_mode:getN("audio_mode"),speechiness:getN("speechiness"),tempo:getN("tempo"),time_signature:getN("time_signature"),audio_valence:getN("audio_valence"),song_duration_ms:getN("song_duration_ms")};try{const r=await fetch(API_URL,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(p)});if(!r.ok){throw new Error("API "+r.status)}const d=await r.json();document.getElementById("scoreValue").textContent=d.predicted_popularity;document.getElementById("modelVersion").textContent=d.model_version;document.getElementById("latency").textContent=d.latency_ms;resultBox.classList.remove("hidden")}catch(err){errorBox.textContent="Failed. Ensure FastAPI runs on :8000. "+err.message;errorBox.classList.remove("hidden")}finally{predictBtn.disabled=false;predictBtn.textContent="Predict Popularity"}});
